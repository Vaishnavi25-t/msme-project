# ============================================================
# application.properties
# Database connection settings
# IMPORTANT: Change "your_password" to your MySQL password
# ============================================================

spring.application.name=msme-backend

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/msme_analyzer
spring.datasource.username=root
spring.datasource.password=Venki@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Run Java server on port 8080
server.port=8082

# Python ML server URL (do not change this)
ml.server.url=http://localhost:5000